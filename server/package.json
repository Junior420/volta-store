{
  "name": "volta-store-backend",
  "version": "1.0.0",
  "description": "Backend API for the Volta electronics store — products, orders and admin panel",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "dev:db": "node dev-mongo.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "express-rate-limit": "^8.5.2",
    "helmet": "^8.2.0",
    "mongodb": "^6.21.0",
    "multer": "^2.0.0"
  },
  "devDependencies": {
    "mongodb-memory-server": "^10.4.3"
  }
}
